// Minimal Prisma schema for core-booking. Models added by the core-booking skill.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
