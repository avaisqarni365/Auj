{
  "name": "@auj/connector-mock",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "dev": "tsx watch src/index.ts",
    "demo": "tsx src/demo.ts"
  },
  "dependencies": {
    "@auj/contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
