// Minimal Prisma schema for payments. Models added by the payments skill.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
