/**
 * Semantic version of the @auj/contracts contract surface.
 * Breaking changes to any interface or domain type require a MAJOR bump;
 * additive, backward-compatible changes a MINOR bump.
 */
export const CONTRACTS_VERSION = '1.1.0' as const;
